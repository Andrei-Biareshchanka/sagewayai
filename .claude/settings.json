{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git commit*)",
            "statusMessage": "sagewayai-reviewer: checking staged changes...",
            "timeout": 120,
            "prompt": "Before this git commit runs, review the staged changes.\n\n1. Run: git diff --cached\n2. Check against SagewayAI rules:\n   - No TypeScript `any` types\n   - Route handlers only: parse input, call lib/Prisma, return JSON — no business logic inline\n   - Always import shared Prisma instance from '../lib/prisma', never instantiate inline\n   - 404 pattern: Object.assign(new Error(msg), { status: 404 }) — no `as` casts\n   - Zod: use .safeParse() not .parse(); use z.coerce.number() for query params; use .issues not .errors\n   - No console.log in committed code\n   - English only — no Cyrillic characters\n   - Functions under 20 lines\n   - /daily route must be declared before /:id\n3. If any .ts files changed, run: cd server && npx tsc --noEmit\n4. If blockers found — output ONLY this JSON (no other text): {\"continue\": false, \"stopReason\": \"sagewayai-reviewer blocked commit:\\n<list each issue on a new line>\"}\n5. If all good — output ONLY this JSON: {\"continue\": true, \"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"sagewayai-reviewer: all checks passed\"}}"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(gh pr create*)",
            "statusMessage": "sagewayai-reviewer: checking branch before PR...",
            "timeout": 120,
            "prompt": "Before this PR is created, review all changes on this branch vs main.\n\n1. Run: git diff main...HEAD\n2. Check against SagewayAI rules:\n   - No TypeScript `any` types\n   - Route handlers only: parse input, call lib/Prisma, return JSON — no business logic inline\n   - Always import shared Prisma instance from '../lib/prisma', never instantiate inline\n   - 404 pattern: Object.assign(new Error(msg), { status: 404 }) — no `as` casts\n   - Zod: use .safeParse() not .parse(); use z.coerce.number() for query params\n   - No console.log in committed code\n   - English only — no Cyrillic characters\n   - Functions under 20 lines\n3. Run: cd server && npx tsc --noEmit\n4. If blockers found — output ONLY this JSON: {\"continue\": false, \"stopReason\": \"sagewayai-reviewer blocked PR:\\n<list each issue>\"}\n5. If all good — output ONLY this JSON: {\"continue\": true, \"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"sagewayai-reviewer: branch looks good\"}}"
          }
        ]
      }
    ]
  }
}
